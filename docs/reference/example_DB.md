# Example CellChat database

The CellChat ligand-receptor database extracted from the example
objects. Store separately and re-attach to objects when running
extract\_cellchat\_data.

## Usage

``` r
example_DB
```

## Format

A list containing the CellChat DB slot
